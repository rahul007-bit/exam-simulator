# EVIDENCE — session-lock-fe

Independent verification evidence. A task may only be marked `verified` when this
file contains real, reproducible proof for every acceptance criterion.

## Implementer — implementer-session-lock-fe (2026-09-11)
- Deliverable: frontend per-session owner lock.
- Files touched:
  - `web/frontend/src/api/client.ts` (cookie + `X-Client-Id` + `getClientId`)
  - `web/frontend/src/api/session.ts` (augmented `SessionInactive.locked?`, local
    `SessionResponse`, token persistence helpers)
  - `web/frontend/src/stores/session.ts` (`isLocked`, token clear on submit/reset/end)
  - `web/frontend/src/stores/index.ts` (resume with persisted token)
  - `web/frontend/src/views/CandidateView.vue` (persist/fetch token, locked card)
  - `web/frontend/playwright.config.ts` (fresh server + dead-port proxy)
  - `web/frontend/tests/unit/session-lock.test.ts` (new)
  - `.agents/session-lock-fe-implementer/*` (metadata)
- Commands run + output:
  ```
  $ bun run lint
  $ eslint .
  (no errors)

  $ bunx vue-tsc --noEmit
  (no output — success)

  $ bun run test
  Test Files  22 passed (22)
       Tests  230 passed (230)
  (includes tests/unit/session-lock.test.ts, 6 tests)

  $ npx playwright test
  Running 20 tests using 8 workers
  20 passed (7.7s)

  [WebServer] ... [vite] http proxy error: /api/session
  [WebServer] Error: connect ECONNREFUSED 127.0.0.1:9
  ```
  The `ECONNREFUSED 127.0.0.1:9` log lines confirm `webServer.env.VITE_BACKEND`
  took effect: the Vite proxy targets the dead port, and `reuseExistingServer:false`
  forces a fresh dev server.
- Screenshots: n/a
- Test cases executed (see TESTPLAN.md): T1 PASS, T2 PASS, T3 PASS, T4 PASS,
  T5 PASS, T6 PASS, T7 PASS
- Self-check against acceptance criteria:
  1. `cka_client_id` cookie ensured/stable + `X-Client-Id` on every request — PASS
     (`getClientId()` returns the cookie value; header asserted in T3).
  2. Candidate token persisted (`cka:candidate-token`), resumed in `initAppStores`
     and `CandidateView` mount, cleared on successful submit/reset/end — PASS.
  3. `locked?: boolean` augmented cleanly; `isLocked` true only for the locked
     inactive payload; locked `data-testid="session-locked"` card with Retry;
     workspace not rendered while locked — PASS.
  4. `reuseExistingServer:false` + `VITE_BACKEND=E2E_BACKEND ?? http://127.0.0.1:9`
     — PASS (proxy error proof above).
  5. New unit test + all existing tests green — PASS (230/230 unit, 20/20 e2e).
- Deviations / notes:
  - Token persistence helpers live in `src/api/session.ts` (shared by the store and
    the view) to avoid a store↔index circular import; no extra files added.
  - `SessionInactive` / `SessionResponse` are re-declared locally in
    `src/api/session.ts` (augmenting the generated schema) rather than editing the
    generated `schema.d.ts`, which is regenerated by `gen:api`.
  - Only the newly added paragraph in `CandidateView.vue` was wrapped to satisfy
    Prettier; two pre-existing formatting nits in that file were left untouched.

## Verifier — <different agent> (<UTC>)
- Clean checkout / environment: <describe>
- Re-ran acceptance commands:
  ```
  <commands and observed output>
  ```
- Test cases re-run independently (see TESTPLAN.md): <T1 PASS, T2 FAIL, ...>
- Criterion-by-criterion result:
  1. <criterion> — PASS/FAIL — <evidence>
- Regression checks (WS, clipboard, timer, a11y, contrast): <result>
- Verdict: `verified` | `rejected` — <reasons if rejected>
