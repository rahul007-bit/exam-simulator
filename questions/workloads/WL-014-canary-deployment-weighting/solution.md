# Solution for WL-014: Deploy Canary Deployment Alongside Production Workload

In namespace `traffic-canary`, deploy `app-v1` (3 replicas) and `app-canary` (1 replica) behind single Service `app-svc` on port 80.
