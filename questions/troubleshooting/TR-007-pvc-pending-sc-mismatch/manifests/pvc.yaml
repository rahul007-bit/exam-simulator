apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: data-claim
  namespace: data-ops
spec:
  storageClassName: fast-nvme-ssd
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
