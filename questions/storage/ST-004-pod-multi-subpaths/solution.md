# Solution for ST-004: Mount Multiple SubPaths from Single PersistentVolume

Create Pod `multi-subpath` in `subpath-multi` mounting volume `data-storage` at `/app/config` (subPath: `config`) and `/app/logs` (subPath: `logs`).
